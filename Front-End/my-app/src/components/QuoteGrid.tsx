//import {useState} from 'react';
import {Grid} from "@mui/material";

export const QuoteGrid = (
    width: "100%",
    margin: "20px auto",
    textAlign: 'center',
    justifyContent: "center",
    backgroundColor: "white",
    ) => {
        const style = {
            width,
            margin,
            textAlign,
            justifyContent,
            backgroundColor,
        }
        return (
            <Grid>Quote Grid</Grid>
        )
    }

